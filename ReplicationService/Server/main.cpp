/*
* #include "common.h"
* #define BUFFER_SIZE 512
#define ORIGINAL_PORT 5000
#define COPY_PORT 5001
#define SERVER_IP_ADDRESS "127.0.0.1"

int main(int argc, char *argv[]) {
    
    if (argc < 2) {
        printf("Usage: server.exe [original|copy]\n");
        return 1;
    }

	return 0;
}
*/

